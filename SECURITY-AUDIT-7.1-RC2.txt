================================================================================
LAPORAN AUDIT KEAMANAN — WordPress 7.1-RC2 (build trunk r63157)
================================================================================
Repo    : kamen-sketch/Wordking-prrss  (mirror byte-per-byte WordPress core)
Basis   : $wp_version = '7.1-RC2-63157', $wp_db_version = 61833
Banding : WordPress 7.0.3 (rilis stabil terakhir) untuk deteksi regresi
Sifat   : Untuk ASESMEN MANUAL. Setiap klaim disertai metode + status paritas 7.0.3.

CATATAN KEJUJURAN METODOLOGIS (baca dulu):
  - Ini core matang. Hampir SETIAP temuan awal berubah jadi pertahanan yang
    bekerja saat ditelusuri ke sumbernya, dan hampir setiap "temuan tingkat
    tinggi" yang sempat muncul ternyata BUG DI ALAT saya, bukan di WordPress.
  - Prinsip: KONTROL POSITIF menentukan. Uji yang "gagal" (0 perubahan / 0
    payload) TIDAK membuktikan aman kecuali dibuktikan alat/harness-nya bisa
    mendeteksi kasus positif. Beberapa kali "hasil bersih" ternyata artefak
    (server mati, denominator salah, SQLite serialisasi).
  - Verifikasi silang mandiri disarankan untuk setiap baris di bawah.

================================================================================
RINGKASAN EKSEKUTIF
================================================================================
Delapan kelas kerentanan diaudit dengan metode independen. NOL kerentanan
exploitable-berseverity-tinggi ditemukan. Yang tersisa:

  [A] 2 bug availability upstream (identik 7.0.3) — dampak rendah
  [B] 1 deprecation PHP 8.4 (identik 7.0.3) — akan fatal di PHP 9
  [C] 2 race condition by-design (identik semua versi) — dampak rendah
  [D] 1 SSRF teoretis via DNS-rebinding TOCTOU — keterbatasan fundamental
  [E] User enumeration by-design — information disclosure rendah

Tidak ada satu pun yang merupakan regresi 7.1. Faktanya 7.1 SEDIKIT LEBIH
diperkeras dari 7.0.3 (commit 428d37e menambah escaping ke jalur error login).

================================================================================
MATRIKS KELAS KERENTANAN — semua NOL
================================================================================
Kelas                     | Metode                                    | Hasil
--------------------------|-------------------------------------------|------
SQL Injection             | 6 alat statis + call graph 12.291 def +   |  0
                          |   38.105 query runtime + 318 payload fuzz |
XSS reflektif             | 791 request fuzz (payload berpenanda)     |  0
XSS tersimpan             | 68 payload x 4 role, 32 respons admin     |  0
XSS konteks-escaping      | sensus AST 8.699 titik output             |  0
CSRF                      | 6 aksi + kontrol positif per-aksi         |  0
XXE                       | uji libxml + baca parser XMP/oEmbed       |  0
Object injection / POP    | telusur 24 unserialize + gadget __destruct|  0
SSRF (pingback/oembed)    | uji IP internal + revalidasi redirect     |  0
Path traversal            | grep file-op dari superglobal + editor    |  0
Open redirect             | grep wp_redirect($_...)                    |  0
Password-reset poisoning  | baca retrieve_password + wp_mail From     |  0 (patched)

================================================================================
[A] BUG AVAILABILITY UPSTREAM (identik 7.0.3, dampak rendah)
================================================================================

A1. class-pclzip.php:3984 — fclose() atas variabel tak terdefinisi
--------------------------------------------------------------------------------
  Kode : fclose($v_file);   // $v_file tidak pernah di-assign di fungsi ini
         (fungsi privExtractFileUsingTempFile, baris 3977-4052)
  Efek : Di PHP 8+, fclose(null) melempar TypeError FATAL (bukan warning).
         @ suppression TIDAK menolongnya (uji: @fclose($undef) tetap TypeError).
  BUKTI: Direproduksi di Chrome headless. Stack trace berakhir di baris 3984.
         Pemicu: PCLZIP_TEMPORARY_DIR tidak writable + entri > threshold
         (memory_limit x 0.47, di-set otomatis oleh privOptionDefaultThreshold).
  KOREKSI PENTING: Baris 2747 (bug serupa) TAK TERJANGKAU — privWriteFileHeader
         selalu return 1. Dan jalur ini TIDAK dicapai WordPress core: core
         memakai extract(PCLZIP_OPT_EXTRACT_AS_STRING) -> privExtractFileAsString
         (nol rujukan temp-file). Hanya terjangkau lewat PclZip::extract() ke
         DISK, mis. plugin pihak ketiga.
  DAMPAK: Rendah. Availability, bukan keamanan. Tidak dapat dipicu penyerang.
  PARITAS: Byte-identik 7.0.3.

A2. wp-admin/includes/export.php — export_wp() mencemari namespace global
--------------------------------------------------------------------------------
  Kode : export_wp() mendeklarasikan 13 fungsi BERNAMA di dalam badan fungsinya:
         wxr_cdata, wxr_site_url, wxr_cat_name, wxr_category_description,
         wxr_tag_name, wxr_tag_description, wxr_term_name, wxr_term_description,
         wxr_term_meta, wxr_authors_list, wxr_nav_menu_terms, wxr_post_taxonomy,
         wxr_filter_postmeta
  Efek : (1) Panggilan KEDUA export_wp() dalam 1 proses -> Fatal "Cannot
             redeclare function wxr_cdata()".
         (2) Plugin yang sudah mendefinisikan salah satu nama itu -> ekspor
             FATAL di panggilan PERTAMA (layar putih di Tools > Export).
         (3) 13 fungsi bocor ke scope global setelah 1 ekspor.
  BUKTI: Direproduksi. `function wxr_cdata(){}` sebelum export_wp() -> Fatal.
         function_exists('wxr_cdata') false->true setelah ekspor.
  DAMPAK: Rendah-Medium. Availability fitur ekspor. Tidak dapat dipicu jarak
         jauh (butuh plugin bernama sama). LEBIH plausibel dari A1.
  PARITAS: Byte-identik 7.0.3.
  CATATAN: PHPStan melaporkan ini (48x "Function wxr_cdata not found", 15x
         "Inner named functions"). Saya sempat SALAH menggugurkannya sebagai
         keterbatasan analisis; ternyata bug runtime nyata.

================================================================================
[B] DEPRECATION PHP 8.4 (identik 7.0.3)
================================================================================

B1. wp-includes/comment.php:2424 — preg_replace(null)
--------------------------------------------------------------------------------
  Kode : $commentdata['comment_author_IP'] =
             preg_replace('/[^0-9a-fA-F:., ]/', '', $commentdata['comment_author_IP']);
  Efek : Memanggil wp_new_comment() TANPA comment_author_IP -> $subject null ->
         "Deprecated: preg_replace(): Passing null to parameter #3 ($subject)".
  BUKTI: Direproduksi (komentar tetap terbuat, tapi dgn notice).
  DOKUMENTASI vs KODE: docblock baris 2137 bilang "Default empty" untuk
         comment_author_IP — tapi default itu TIDAK PERNAH diterapkan di kode.
  DAMPAK: Sekarang hanya notice. Di PHP 9.0, passing null ke param internal
         non-nullable menjadi TypeError FATAL. Terkena: pembuatan komentar
         programatik tanpa IP (WP-CLI, importer, cron, plugin). Request web
         biasa aman ($_SERVER['REMOTE_ADDR'] terisi).
  PARITAS: Byte-identik 7.0.3 (baris 2366 di sana).

================================================================================
[C] RACE CONDITION BY-DESIGN (identik semua versi, dampak rendah)
================================================================================

C1. wp_insert_user — TOCTOU duplikat username
--------------------------------------------------------------------------------
  FAKTA 1 (TERBUKTI): wp_users TIDAK punya UNIQUE constraint pada user_login.
         Skema (schema.php:204): "KEY user_login_key (user_login)" = non-unik.
         Uji langsung: dua INSERT user_login identik -> KEDUANYA sukses, 2 baris
         tersimpan. DB tidak menolak. Identik dgn MySQL (skema sama).
  FAKTA 2 (TERBUKTI, baca kode): user.php:2325
         if (!$update && username_exists($user_login)) return WP_Error(...);
         ... lalu INSERT.  = check-then-act tanpa penguncian.
  KESIMPULAN: FAKTA 1 + FAKTA 2 = TOCTOU secara definisi. Dua registrasi
         konkuren nama sama bisa lolos check bersamaan -> duplikat username.
  BATAS PENGUJIAN (JUJUR): Race menyala saat runtime TIDAK dapat saya
         demonstrasikan di environment ini. Alasan:
           - MySQL sungguhan tak bisa dipasang: Docker Hub, apt Ubuntu, pypi,
             npm, golang-proxy SEMUA diblokir (403). Hanya git-clone jalan.
           - SQLite (drop-in uji) menserialkan tulis -> window tak muncul.
           - Startup proses PHP (~300ms) melebihi window; proses stagger.
           - Drop-in mencegat `new wpdb()` -> tak bisa 2 koneksi terpisah.
         Ini keterbatasan LINGKUNGAN, BUKAN bukti race tidak ada.
  DAMPAK: Rendah. get_user_by('login') pakai LIMIT 1 (baris pertama menang) ->
         isu integritas data, bukan privilege escalation. Penyerang hanya bisa
         "balapan dgn dirinya" (nama existing tertangkap check yg sudah commit).
  KONTRAS (kenapa ini by-design): add_option (option.php:1142) memakai
         "INSERT ... ON DUPLICATE KEY UPDATE" atas UNIQUE option_name = upsert
         ATOMIK. WordPress TAHU cara benar; memilih tidak untuk user karena
         duplikat ditoleransi. Keputusan sadar, bukan kelalaian.
  PARITAS: Skema identik 7.0.3 dan semua versi.

C2. spawn_cron — TOCTOU double-execution cron
--------------------------------------------------------------------------------
  Kode : cron.php:915  $lock = get_transient('doing_cron');   // cek
         cron.php:943  set_transient('doing_cron', ...);       // tulis
         Transient tidak atomik -> window antara cek dan tulis.
  MITIGASI: compare-after-set. wp-cron.php membaca ulang token $doing_wp_cron
         dan hanya proses yang menang lock yang lanjut. Best-effort.
  DAMPAK: Rendah. Worst case satu cron job jalan dua kali. WP sengaja tidak
         pakai DB locking di sini.
  PARITAS: identik semua versi.

CATATAN kelas race: TIDAK ADA pola "SET x = x + 1" (increment SQL non-atomik)
  di seluruh core (grep kosong). Semua counter (comment_count, term count)
  pakai recount SELECT COUNT(*) lalu UPDATE = N -> IDEMPOTEN, aman dari race.

================================================================================
[D] SSRF TEORETIS — DNS-rebinding TOCTOU
================================================================================
  Konteks: pingback.ping (class-wp-xmlrpc-server.php:7072) dan oEmbed proxy
         memakai wp_safe_remote_get (tervalidasi wp_http_validate_url).
  PERTAHANAN (terbukti, uji langsung):
    - Blocklist komprehensif: 127/8, 10/8, 172.16/12, 192.168/16, 169.254/16
      (link-local + CLOUD METADATA), CGNAT, TEST-NET, multicast, reserved.
      Uji: 169.254.169.254, 10.0.0.1, [::1], 0177.0.0.1 (oktal),
      2130706433 (desimal) -> SEMUA DIBLOKIR. Bypass encoding gagal.
    - Revalidasi redirect TIAP HOP: class-wp-http.php:353 daftar
      validate_redirects sbg hook requests.before_redirect; throw bila gagal.
    - Pengecualian loopback HANYA untuk host situs sendiri (uji: dgn
      home=example.com, 127.0.0.1 & localhost DIBLOKIR).
  CELAH TERSISA: DNS rebinding. wp_http_validate_url resolve via gethostbyname
         sekali; fetch resolve ULANG. Penyerang dgn DNS TTL sangat rendah bisa
         balas IP publik ke validasi, IP internal ke fetch.
  DAMPAK: Rendah. Keterbatasan FUNDAMENTAL validasi-resolusi-sekali (bukan bug
         7.1). Butuh DNS terkontrol penyerang. Tak bisa ditambal tanpa pin-IP
         yang tak dilakukan transport PHP mana pun.
  CATATAN: Blocklist 169.254 dari commit 7.1 c794a2e "HTTP API: Improve
         compliance with IPv4 Special-Purpose Address Space" = hardening aktif.

================================================================================
[E] USER ENUMERATION (by-design, information disclosure rendah)
================================================================================
  Terbukti live (unauth):
    /?author=1                    -> 301 /author/admin/     (bocor username ID 1)
    /wp-json/wp/v2/users          -> ['admin', ...]         (bocor semua author)
    /oembed/1.0/embed             -> author_name: admin
  DAMPAK: Rendah. Perilaku TERDOKUMENTASI & disengaja WordPress (arsip author
         fitur inti; REST /users hanya author yg punya post terpublikasi).
         Bukan kelas CVE. Mitigasi = urusan plugin/konfigurasi.

================================================================================
CATATAN PENUTUP UNTUK ASESOR
================================================================================
1. Repo ini mirror byte-per-byte WordPress core, NOL modifikasi lokal. HEAD
   persis di upstream (tree hash identik). Tidak ada "kode kamu" yang bisa
   punya bug — semua adalah kode WordPress yang sudah lewat review kontributor.

2. Risiko OPERASIONAL utama BUKAN salah satu temuan di atas, melainkan: ini
   7.1 RELEASE CANDIDATE, bukan rilis stabil. RC secara definisi masih masa
   perburuan bug. Untuk produksi: pakai 7.0.3 (stabil terakhir).

3. Semua perkakas audit ada di /workspace (di luar repo): instalasi WordPress
   berjalan di /workspace/wpsite (SQLite drop-in + instrumentasi query), dan
   scanner di scratchpad. Tidak di-commit ke repo ini agar mirror tetap bersih.

4. Untuk memverifikasi C1 (race) secara runtime, perlu environment dgn MySQL/
   MariaDB nyata (row-level locking) — tidak tersedia di sini. Harness siap.

--------------------------------------------------------------------------------
Dihasilkan oleh sesi audit Claude Code. Untuk asesmen manual — verifikasi
silang tiap baris disarankan.
================================================================================

================================================================================
[F] TEMUAN REACHABLE UNAUTHENTICATED / SUBSCRIBER (iterasi terarah)
================================================================================
Metode: enumerasi permukaan unauth (10 REST route publik, 0 nopriv-AJAX,
xmlrpc, wp-trackback, wp-login, wp-cron, pingback) + PoC dijalankan + kontrol
positif. Semua temuan BY-DESIGN, low-medium severity, identik semua versi WP.

F1. XML-RPC system.multicall — amplifikasi brute-force (UNAUTH)  [VERIFIED]
--------------------------------------------------------------------------------
  Root cause: IXR-server.php:205 hanya memblokir multicall REKURSIF, tidak
    memblokir bundel wp.getUsersBlogs (tiap panggilan = 1 percobaan login via
    wp_authenticate, class-wp-xmlrpc-server.php:308). Tak ada rate-limit core.
  PoC (dijalankan, HTTP nyata port 8090):
    - 20 password distinct dalam 1 request multicall -> 20 sub-respons (4467 B).
    - Kontrol positif: password BENAR -> <isAdmin>1</isAdmin> (sukses oracle),
      salah -> faultCode 403 "Incorrect username or password".
  Exploitable: YA, unauth. Bypass rate-limiter per-HTTP-request (fail2ban lihat
    1 request, bukan 20). Juga vektor DoS amplifikasi.
  Evidence: 1 request HTTP -> 20 pemeriksaan kredensial, tiap hasil terbedakan.
  Status: BY-DESIGN. IXR multicall byte-identik 7.0.3. Mitigasi = disable
    xmlrpc / plugin / host (posisi resmi WordPress).
  Anomali dicatat: multicall dgn password benar berulang kadang hanya 1 sukses
    (kemungkinan stale $this->error state antar sub-call = mitigasi tak sengaja).

F2. Username enumeration via pesan error login (UNAUTH)  [VERIFIED]
--------------------------------------------------------------------------------
  Root cause: user.php:188 "The username X is not registered" (user TIDAK ada)
    vs user.php:215 "The password you entered for the username X is incorrect"
    (user ADA). Pesan berbeda = oracle.
  PoC (live): admin -> pesan-password (ada); nouser_xyz -> pesan-username
    (tidak ada); idor_author -> pesan-password (ada).
  Exploitable: YA, unauth. Bedakan username valid/invalid.
  Status: BY-DESIGN (usability). Filter 'login_errors' (wp-login.php:280) bisa
    menyeragamkan, tapi DEFAULT membocorkan. Sekelas dgn enum author-archive &
    REST /wp/v2/users & oembed author_name. WordPress: username bukan rahasia.

F3. Pingback external SSRF (UNAUTH)  [VERIFIED]
--------------------------------------------------------------------------------
  Root cause: pingback.ping (xmlrpc) memanggil wp_safe_remote_get atas
    sourceURI attacker. Internal/metadata DIBLOKIR wp_http_validate_url, tapi
    host EKSTERNAL diizinkan (inheren utk pingback).
  PoC: pingback ke 127.0.0.1:9999 -> TAK ADA outbound (internal blokir).
    wp_http_validate_url("8.8.8.8") -> LOLOS (eksternal). Situs bisa dipaksa
    GET ke host eksternal arbitrer (port scan eksternal / blind GET proxy).
  Exploitable: terbatas. Eksternal-saja, blind-ish (hanya ungkap apakah target
    memuat link balik). Internal/cloud-metadata AMAN (F lihat [D]).
  Status: BY-DESIGN. Pertahanan internal-IP + revalidasi redirect (lihat [D]).

--- VERIFIED SAFE terhadap unauth/subscriber (false-positive dieliminasi) ---
  - Abilities API (/wp-abilities/v1): unauth 401 semua; subscriber HANYA
    core/get-user-info (data DIRI SENDIRI, tanpa user_pass, tanpa pivot user_id);
    env-info/site-info butuh manage_options. 5 PoC.
  - REST /batch/v1: per-subrequest auth (create-admin -> 401); limit 25.
  - REST /oembed/1.0/embed: post privat/draft -> 404 (tak bocor).
  - REST /wp/v2/types ctx=edit: 401. /wp/v2/search: post privat tak bocor.
  - IDOR lintas-objek (subscriber): 8/8 ditolak (rest_cannot_edit/delete/
    forbidden_context) + kontrol positif (edit post sendiri -> 200).
  - Privilege escalation (subscriber): role field -> rest_cannot_edit_roles;
    wp_capabilities meta tak REST-writable; + kontrol positif (admin promote OK).
  - Stored XSS blok baru (playlist/tabs) via author low-priv: kses MELUCUTI
    atribut JSON jahat & onerror saat simpan (kontrol positif: author dilucuti,
    admin bertahan via unfiltered_html). Escaper blok = lapis kedua.
  - wp-trackback.php: sanitize_url/text/textarea + charset whitelist
    mb_list_encodings + UTF-7 blokir; tb_id (int); DB prepare %d/%s.
  - wp-cron.php: unauth by-design, ter-lock transient doing_cron.

================================================================================
[G] ITERASI DIFF-DRIVEN 7.0.3 -> 7.1 (baca kode yang BERUBAH, bukan ingatan)
================================================================================
Metode: `diff -rq wp-includes` 7.0.3 vs 7.1-RC2 -> tiap file .php yang BERUBAH
dan unauth/subscriber-reachable ditelaah dari sumber + PoC runtime (internal
rest_do_request + unit filter). Tidak ada file .php yang benar-benar baru
(ai-client & abilities-api sudah ada di 7.0.3, hanya berubah).

G1. core/comment-author-name block — perubahan sprintf %1s -> %1$s  [SAFE]
--------------------------------------------------------------------------------
  Root cause dugaan: $comment_author = get_comment_author() ditaruh ke <div>
    TANPA esc_html (comment-author-name.php:52). Nama komentar = input unauth.
  Analisis: perubahan 7.1 hanya kosmetik. `%1s` di PHP = lebar-minimum-1 yang
    mengonsumsi argumen SECARA URUT -> 7.0.3 kebetulan sudah benar & ter-escape
    (esc_url/esc_attr). `%1$s` versi 7.1 = sintaks posisional yang benar. Nihil
    beda keamanan.
  PoC (unit, dijalankan): apply_filters('pre_comment_author_name', <payload>):
    '<script>alert(1)</script>' -> ''(kosong)
    '"><svg onload=alert(1)>'   -> '\"&gt;'  (tag dilucuti, > jadi entity)
    'John<img onerror=..>'       -> 'John'
    Rantai input: sanitize_text_field + wp_filter_kses + _wp_specialchars(30).
    Output = konteks teks (bukan atribut). Tag mustahil terbentuk. SAFE.

G2. Abilities API RUN endpoint — enforcement izin  [SAFE, live-tested]
--------------------------------------------------------------------------------
  Root cause dicek: run-controller permission_callback=check_ability_permissions
    (baris 143) memanggil $ability->check_permissions($input) SEBELUM execute;
    REST core selalu jalankan permission_callback dulu. Fail-closed penuh.
  PoC (internal rest_do_request, dijalankan live):
    UNAUTH  GET .../get-site-info/run        -> 401 rest_ability_cannot_execute
    UNAUTH  GET .../get-environment-info/run -> 401
    UNAUTH  GET .../get-user-info/run        -> 401
    SUBSCR  GET .../get-site-info/run        -> 403 (butuh manage_options)
    SUBSCR  GET .../get-environment-info/run -> 403
    SUBSCR  GET .../get-user-info/run        -> 200, keys = id,display_name,
      user_nicename,user_login,roles,locale,first_name,last_name,nickname,
      description,user_url. user_pass:no  email:no.
    UJI PIVOT (subscriber kirim user_id=1,id=1) -> tetap balik login SENDIRI
      ('idor_sub'), BUKAN admin. execute_callback pakai wp_get_current_user();
      input additionalProperties:false -> parameter asing diabaikan. TAK ADA IDOR.
  Catatan metode: 3 ability core semua readonly -> endpoint minta GET (POST=405
    rest_ability_invalid_method via validate_request_method). Perbaikan dari
    iterasi sebelumnya yang keliru pakai POST.

G3. Widget instance unserialize() x2 — object injection  [SAFE]
--------------------------------------------------------------------------------
  Sink: class-wp-rest-widget-types-controller.php:498 & widgets-controller.php:589
    `unserialize( base64_decode($request['instance']['encoded']) )`.
  Guard: didahului hash_equals( wp_hash($serialized), $request['instance']['hash'] ).
    wp_hash = HMAC dgn secret salt situs -> blob TIDAK bisa dipalsukan attacker.
    Plus permission edit_theme_options (admin). Double-gated. POP gadget mustahil
    dicapai unauth/subscriber.

G4. icons REST controller  [SAFE]
--------------------------------------------------------------------------------
  get_items_permissions_check butuh edit_posts -> unauth/subscriber 401.
  get_item (single) permission=true tapi hanya kembalikan markup SVG statis
    terdaftar (aset publik, non-sensitif). By-design.

G5. ai-client HTTP client  [SAFE / tidak ter-reach]
--------------------------------------------------------------------------------
  Diff hanya kosmetik (format pesan error tambah %s method). Tidak ter-wire ke
    REST/abilities publik -> tidak reachable unauth/subscriber.

G6. Sink berbahaya lain (scan diff)  [SAFE]
--------------------------------------------------------------------------------
  eval/assert/create_function/proc_open/shell_exec: nihil di jalur request core
    (hanya popen di PHPMailer sendmail + assert PHPStan di SimplePie).
  preg_replace: tak ada modifier /e.
  REST anonim komentar: create_item_permissions_check -> filter
    rest_allow_anonymous_comments DEFAULT false -> unauth 401 (live-confirmed),
    independen dari comment_registration. Jalur unauth nyata = wp-comments-post.php
    (sanitasi author _wp_specialchars + konten kses, sudah diverifikasi).

KESIMPULAN [G]: seluruh kode yang BERUBAH 7.0.3->7.1 dan reachable dari
unauth/subscriber telah ditelaah dari file + PoC runtime. Tidak ditemukan
kerentanan exploitable baru. Abilities API (subsistem paling baru) terbukti
robust termasuk uji pivot aktif yang gagal. Temuan by-design F1/F2/F3 tetap.
